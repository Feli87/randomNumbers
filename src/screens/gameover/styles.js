import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    button:{
      width: 250,
      height: 60,
      backgroundColor: '#DDD',
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 15,
      color: "black",
      borderWidth: 5,
      borderColor: 'gray',
      padding: 5
  },
  restartGameText:{
    fontSize: 20,
    fontWeight: 'bold',
    color: 'black',
  }
  });